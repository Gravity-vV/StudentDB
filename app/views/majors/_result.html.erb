<p id="notice"><%= notice %></p>

<h4><%=@major.name %></h4>

<table>
  <thead>
  <tr>
    <th>课程</th>
    <th>课程号</th>
    <th>性质</th>
    <th>学分</th>
    <th>开设时间</th>
    <th>详情</th>
    <th colspan="6"></th>
  </tr>
  </thead>

  <tbody>
  <% @courses.each do |c| %>
    <tr>
      <td><%= c.name %></td>
      <td><%= c.cno %></td>
      <td><%= MajorsCourses.where('major_id = ? AND course_id = ? ',@major.id,c.id ).first.compulsory ? '必修': '选修' %></td>
      <td><%= MajorsCourses.where('major_id = ? AND course_id = ? ',@major.id,c.id ).first.credit %></td>
      <td><%= MajorsCourses.where('major_id = ? AND course_id = ? ',@major.id,c.id ).first.start_time %></td>
      <td><%= link_to 'show',course_path(c) %></td>
<!--      <td><%#= link_to 'Show', major %></td>-->
<!--      <td><%#= link_to 'Edit', edit_major_path(major) %></td>-->
<!--      <td><%#= link_to 'Destroy', major, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
    </tr>
  <% end %>
  </tbody>
</table>

<br>


<%#= link_to 'Back', @major %>
<%#= link_to 'New Major', new_major_path %>
